# Volumes

### `blobcache ivol <volh>`
Short for "inspect volume", this command prints information about the Volume to stdout.
It requires a valid handle to the Volume.

#### Positional Arguments
- `volh` The handle to the Volume.

## `blobcache mkvol.____`
Short for "make volume", these commands all create a new volume in the system.

### `mkvol.local`
Creates a new volume on the local node.
This is the most basic kind of volume in Blobcache.

#### Positional Arguments

#### Flag Arguments
All flags are optional and have sensible defaults.

- `--host` the endpoint of the node to perform the operation on (optional).
- `--hash` the hash algorithm used by the volume to determine `CIDs` (default: `BLAKE3_256`).
- `--max-size` the maximum size (in bytes) of blobs in the volume (the cell typically uses up to half this value).

### `mkvol.remote <endpoint> <volid>`
Creates a remote volume on the local Node.
A remote volume is a Volume on another Node, but on the remote Node it won't be a remote volume, it would be a local volume, or some other kind of primitive volume.
It's only called a "remote" Volume on the local node.

#### Positional Arguments
- `endpoint` the endpoint of the node hosting the target volume.
- `volid` the `OID` of the volume on the remote node.

### `mkvol.vault <inner-volh>`
Vault Volumes encrypt all of their contents including both the cell and all the blobs.
Vault Volumes require an inner Volume to be specified, which they will store their content in.

#### Positional Arguments
- `inner-volh` A handle to the inner volume that will store the ciphertext blob and cell data.


### `open-fiat <oid> [mask]`
Opens a handle to an object by `OID`, subject to the provided access mask.
Prints the resulting handle.

#### Positional Arguments
- `oid` The object identifier.
- `mask` Optional hex-encoded access mask; defaults to all rights when omitted.

### `open-from <volh> <oid> [mask]`
Opens a handle to an object by `OID`, relative to a base volume handle, subject to the provided access mask.
Prints the resulting handle.

#### Positional Arguments
- `volh` A base volume handle.
- `oid` The object identifier within the base volume's context.
- `mask` Optional hex-encoded access mask; defaults to all rights when omitted.

### `await <volh>`
Waits for a volume to change (by condition).

> NOTE: not yet implemented.

### `drop <handle>`
Drops/releases a handle so the server may reclaim resources sooner.
